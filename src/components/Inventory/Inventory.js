import React from 'react';

const Inventory = () => {
    const myStyle ={
        margin: '20px',
    }
    return (
        <div style={myStyle}>
            <h1>This is Inventory section.</h1>
        </div>
    );
};

export default Inventory;